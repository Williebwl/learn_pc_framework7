define(['core.view', 'System/Dept/dept.service.js'],
    function (core) {
        'use strict'

        core.controller('DeptViewCtrl',
            function ($scope, institutionDeptService) {

            })
    })